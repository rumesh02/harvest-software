# Suppress deprecation warnings for util._extend until dependencies are fully updated
# This is safe for collaborative work as it doesn't change functionality
no-deprecation=true

# Alternative: Only show deprecation warnings for our own code
# deprecation=false
